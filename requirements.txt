# --- the app ---
moderngl>=5.8.0
glfw>=2.6.0
imgui-bundle>=1.0.0
numpy>=1.24.0
Pillow>=10.0.0
pyopengl>=3.1.0

# --- the search pilot (pilot/, docs/SEARCH.md) ---
# Only needed to run a search; the app itself does not import these.
# scipy: the texture backend's Euler-characteristic curve, which is what
# separates spots from labyrinths -- the Turing morphology axis.
scipy>=1.10.0
# For backend='clip', add torch + open_clip_torch. INSTALL TORCH FROM THE CUDA
# INDEX, not from PyPI: a plain `pip install torch` silently gives the CPU-only
# wheel, which works but embeds a generation about 30x slower.
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   pip install open_clip_torch
#
# cu128 (CUDA 12.8) is the minimum for Blackwell cards (RTX 50-series,
# compute capability sm_120); older cu121 wheels will not run on them at all.
# Check it took with:
#   python -c "import torch; print(torch.cuda.is_available())"